---
description: Auto-run terminal commands
---
// turbo-all
1. This workflow defines that all terminal commands should be executed without user prompt if the user has enabled the "Always proceed" setting.
