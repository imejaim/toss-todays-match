# 📋 프로덕션 출시 체크리스트 (Production Checklist)

**작성일**: 2026-01-18  
**버전**: v1.0.1  
**목적**: 토스 인앱 출시를 위해 **반드시** 확인/수정해야 하는 사항들을 정리

---

## ✅ 이미 완료된 사항 (재확인 불필요)

| 항목 | 상태 | 파일/위치 | 비고 |
|------|:----:|-----------|------|
| 핀치줌 비활성화 | ✅ | `index.html` | `user-scalable=no` 설정됨 |
| 내비게이션바 아이콘 HTTPS | ✅ | `granite.config.ts` | GitHub raw URL 사용 |
| 중복 내비게이션바 제거 | ✅ | 전체 페이지 | 수동 ← 버튼 없음 |
| 라이트모드 전용 | ✅ | UI 전체 | 다크모드 미구현 |
| 광고 ID 설정 | ✅ | `useRewardedAd.ts` | `ait.v2.live.*` 사용 중 |
| 광고 사전 로드(Preload) | ✅ | `useRewardedAd.ts` | `useEffect`로 마운트 시 로드 |
| 광고 이벤트 수신 | ✅ | `useRewardedAd.ts` | `onEvent` 콜백 처리 |
| 리워드 완료 후 보상 지급 | ✅ | `useRewardedAd.ts` | `userEarnedReward` 이벤트 처리 |
| 개인정보 처리방침 페이지 | ✅ | `src/pages/Privacy.tsx` | 기본 템플릿 구현됨 |
| TDS 스타일 준수 | ✅ | 전체 | 커스텀 `ui.tsx` 사용 |

---

## ✅ 광고 로직 - 코드 수정 불필요! (자동 환경 분기)

> [!IMPORTANT]
> **광고 코드는 이미 완벽하게 구현되어 있습니다. 출시 전 수정하지 마세요!**

### 📌 현재 구현 상태 (건드리지 말 것!)

**📄 파일**: `src/hooks/useRewardedAd.ts`

광고 로직은 **`isTossApp`** 변수로 환경을 자동 감지하여 분기 처리됩니다:

```typescript
// Line 30: 토스 앱 환경 자동 감지
const isTossApp = typeof GoogleAdMob !== 'undefined' && GoogleAdMob?.loadAppsInTossAdMob;
```

| 환경 | `isTossApp` 값 | 광고 동작 | 보상 지급 |
|------|:--------------:|----------|----------|
| **localhost (웹 브라우저)** | `false` | ❌ 스킵 | ✅ 0.5초 후 자동 |
| **토스 샌드박스 앱** | `true` | ✅ 실제 광고 | ✅ 시청 완료 시 |
| **토스 라이브 앱** | `true` | ✅ 실제 광고 | ✅ 시청 완료 시 |

### ⚠️ 왜 수정하면 안 되나요?

1. **로컬 개발**: 웹에서 광고 SDK 로드 불가 → 자동 스킵 + 자동 보상으로 개발 편의 제공
2. **토스 앱**: `GoogleAdMob` 런타임 주입 → 실제 광고 자동 작동
3. **빌드 파일(.ait)**: 동일한 코드가 환경에 따라 다르게 동작

### ✅ 출시 전 확인만 하세요 (토스 샌드박스에서)

| 테스트 항목 | 예상 결과 | 확인 |
|------------|----------|:----:|
| 프리미엄 보고서 진입 | "광고 보고 열기" 버튼 표시 | ☐ |
| 버튼 클릭 | 실제 광고 로딩 및 재생 | ☐ |
| 광고 시청 완료 | 프리미엄 보고서 해금 | ☐ |
| 광고 중도 종료 | 보상 미지급, 버튼 재표시 | ☐ |

---

## 🔴 출시 전 실제 수정/확인 필요 사항

---

### 2. 광고 ID 유효성 확인

**📄 파일**: `src/hooks/useRewardedAd.ts` (Line 11)

```typescript
const TEST_AD_GROUP_ID = 'ait.v2.live.f7cf74bd6b6b4c55';
```

**확인 방법**:
1. [토스 개발자 콘솔](https://developers-apps-in-toss.toss.im/) 접속
2. 수익화 > 인앱광고 메뉴
3. 위 ID가 등록되어 있고 활성화 상태인지 확인

---

### 3. 개인정보 처리방침 URL 등록 (콘솔)

- **콘솔 위치**: 앱 정보 > 기본 정보 > 개인정보 처리방침 URL
- **등록할 URL**: `https://todays-match.apps.tossmini.com/privacy`

---

### 4. 버전 업데이트 (새 빌드 제출 시)

**📄 파일**: `package.json` (Line 4)

```json
"version": "1.0.1",  // 제출할 때마다 버전 올리기
```

---

## 🧪 출시 전 테스트 시나리오

### 광고 테스트 (토스 샌드박스 필수)

| 단계 | 예상 결과 | 확인 |
|------|----------|:----:|
| 1. 프리미엄 보고서 진입 | 💎 아이콘과 "광고 보고 열기" 버튼 표시 | ☐ |
| 2. 버튼 클릭 | 광고 로딩 (잠깐 시간 소요 가능) | ☐ |
| 3. 광고 시청 완료 | 프리미엄 보고서 해금, AI 분석 표시 | ☐ |
| 4. 광고 중도 종료 (X 버튼) | 보상 미지급, 버튼 다시 표시 | ☐ |

---

## 📝 빌드 및 제출 명령어

```bash
# 1. 린트 및 테스트
npm run qc

# 2. 프로덕션 빌드
npm run build

# 3. 생성된 파일 확인
# → todays-match.ait (프로젝트 루트)

# 4. 토스 콘솔에 업로드
# → 버전 관리 > 새 버전 만들기 > .ait 파일 업로드
```

---

## 📚 참고 문서

- `reference/4_출시/app-nongame.md` - 비게임 출시 가이드 (반려 방지)
- `reference/4_출시/deploy.md` - 배포 프로세스
- `docs/04_REVIEW_GUIDE.md` - 심사 제출 시 작성 내용
- `LAUNCH_GUIDE.md` - 광고 연동 상세 가이드

---

## 🖼️ 이미지 관리 (앱 재출시 불필요!)

> [!TIP]
> **이미지는 GitHub에서 로드됩니다. 새 이미지 추가 시 앱 재출시가 필요 없습니다!**

### 현재 구조

```
public/match_images/
├── male/          (남성 이상형 - 여성 사용자에게 표시)
│   ├── wood_01.png ~ wood_03.png   ← 나무(木)
│   ├── fire_01.png ~ fire_03.png   ← 불(火)
│   ├── earth_01.png ~ earth_03.png ← 흙(土)
│   ├── metal_01.png ~ metal_03.png ← 쇠(金)
│   └── water_01.png ~ water_03.png ← 물(水)
└── female/        (여성 이상형 - 남성 사용자에게 표시)
    └── (동일 구조)
```

### 이미지 추가 방법

1. **파일 네이밍**: `{오행영문}_{번호}.png`
   - 예: `wood_04.png`, `fire_05.png`
2. **GitHub에 Push**: `public/match_images/{gender}/` 폴더에 추가
3. **코드 수정**: `PremiumReport.tsx`에서 랜덤 범위 조정
   ```typescript
   const variant = Math.floor(Math.random() * 5) + 1;  // 1~5로 확장
   ```

### GitHub 용량 제한

| 항목 | 제한 | 현재 사용량 |
|------|------|------------|
| 개별 파일 | 100MB 이하 | 약 700KB/개 ✅ |
| 저장소 전체 | 권장 1GB | 약 21MB ✅ |
| **추가 가능** | 약 1,400개 이상 | 여유로움 ✅ |

### 이미지 로드 URL

```
https://raw.githubusercontent.com/imejaim/toss-todays-match/main/public/match_images/{gender}/{element}_{variant}.png
```

**수정일**: 2026-01-18 (v1.0.2)

---

*마지막 업데이트: 2026-01-18*
